<%= form_with(model: fb_event) do |form| %>
  <% if fb_event.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fb_event.errors.count, "error") %> prohibited this fb_event from being saved:</h2>

      <ul>
        <% fb_event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data, style: "display: block" %>
    <%= form.text_field :data %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

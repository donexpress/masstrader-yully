<%= form_with(model: conversation) do |form| %>
  <% if conversation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
        <% conversation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='mb-2'>
    <%= form.label :client_phone_number, style: "display: block", class: 'font-bold' %>
    <%= form.text_field :client_phone_number, placeholder: '56940234906', class: 'w-full rounded-lg' %>
  </div>

  <div class='bg-sky-600 text-slate-50 p-4 text-center rounded-lg'>
    <%= form.submit %>
  </div>
<% end %>

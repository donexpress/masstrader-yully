<p style="color: green"><%= notice %></p>

<div class="flex justify-between w-full">
  <h1 class='text-3xl font-bold underline'>Conversations</h1>

  <%= link_to "+ Conversation", new_conversation_path, class: 'p-2 bg-teal-500 rounded-md text-slate-50' %>
</div>


<div id="conversations">
  <% @conversations.each do |conversation| %>
    <%= render conversation %>
  <% end %>
</div>
